name: live2text CI

on:
  push:
    branches:
      - main
  pull_request:

env:
  GOLANG_VERSION: '1.24.3'
  ACTIONS_STEP_DEBUG: true

concurrency:
  cancel-in-progress: true
  group: ${{ github.ref }}

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      hub: ${{ steps.print.outputs.hub }}
      envi: ${{ steps.print.outputs.envi }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Setup Go environment
        uses: actions/setup-go@v5
        with:
          go-version: "^${{ env.GOLANG_VERSION }}"

      - name: Print Go version
        id: print
        run: | 
          go version
          echo "envi=hello" >> "$GITHUB_OUTPUT"
          echo "hub=\"$(echo '${{ toJSON(github) }}' | tr -d '*' | base64 -w 0)\""
          echo "hub=\"$(echo '${{ toJSON(github) }}' | tr -d '*' | base64 | head -10 | tr -d $'\n')\"" >> "$GITHUB_OUTPUT"

  use-output:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - run: |
          echo "hub: ${{ needs.build.outputs.hub }}"
          echo "HUB: $HUB"
          echo "ENVI: $ENVI"
          echo "envi: ${{needs.build.outputs.envi}}"
        env:
          HUB: ${{needs.build.outputs.hub}}
          ENVI: ${{needs.build.outputs.envi}}

